import type { APIRoute } from 'astro';
export const GET:APIRoute=()=>{const site=(import.meta.env.SITE_URL||'http://localhost:4321').replace(/\/$/,'');const base=import.meta.env.BASE_URL.replace(/\/$/,'');return new Response(import.meta.env.PUBLIC_LIVE_SITE==='true'?`User-agent: *\nAllow: ${base||'/'}\nSitemap: ${site}${base}/sitemap-index.xml\n`:'User-agent: *\nDisallow: /\n',{headers:{'Content-Type':'text/plain'}})};
